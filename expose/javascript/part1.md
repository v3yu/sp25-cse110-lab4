1. 20
2. 20
3. Because the variable's scope is inside the function definition, which means we can access it even when the if statement it is inside does not execute.
4. 20
5. ReferenceError. This is because let result = 0's scope is inside of the if statement, so the second console.log cannot access it.
6. TypeError. We cannot assign values to a const variable.
7. Nothing, because the program stopped when the previous error was caught.
